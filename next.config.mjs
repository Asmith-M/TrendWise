/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'lh3.googleusercontent.com',
      // Add any other image domains you use here
    ],
  },
  // Other Next.js configurations can go here
};

export default nextConfig; // <--- Change to this 